---
import CotizarCompararBtns from "./CotizarCompararBtns.astro";
interface Props {
  title: string;
  text?: string;
  heroImage: string;
  btns?: boolean;
  titleColor?: string;
  textColor?: string;
  bgColor?: string;
  subTitle?: string;
}
const {
  title,
  text,
  heroImage,
  btns,
  titleColor = "black",
  textColor = "#374151",
  bgColor,
  subTitle
} = Astro.props as Props;
---

<section
  class="w-full mt-16 h-auto md:h-[90vh] flex flex-col justify-end items-end gap-5 md:gap-10 py-10"
  style={bgColor ? { backgroundColor: bgColor } : undefined}
>
  <div class="w-full h-[50%] flex justify-center items-end relative">
    <div class="absolute inset-0 h-[75%]">
      <div class="w-full h-[60%]"></div>
      <div
        class="bg-gradient-to-t from-gray-400/50 via-gray-400/30 to-transparent h-[40%]"
      >
      </div>
    </div>
    <img
      src={heroImage}
      alt="Hero Image"
      class="z-10 w-[80%] h-auto md:w-auto md:h-full md:max-w-[800px] object-contain"
    />
  </div>
  
  <div class="w-full h-[50%] text-center flex flex-col justify-center">
    <h1
      class="text-2xl md:text-5xl font-bold max-w-[45ch] mx-auto uppercase pb-5"
      style={{ color: titleColor }}
    >
      {title}
    </h1>
    <h2 class="text-white font-bold text-xl md:text-2xl">{subTitle}</h2>
    <br />
    <slot />
    <br>
    <p
      class="text-sm md:text-xl max-w-[150ch] mx-auto"
      style={{ color: textColor }}
    >
      {text}
    </p>
    
    <!--SLOT HERE -->

    {
      btns ? (
        <br/>
        <CotizarCompararBtns cotizarHref="/Contact" compararHref="#" />
      ) : (
        ""
      )
    }
  </div>
</section>

---
export interface Props {
  text?: string;
  className?: string;
  href?: string;
  isExternal?: boolean;
  label?: string;
}

const {
  text = "Learn More",
  className = "bg-[#14427c] hover:bg-transparent hover:text-[#14427c]",
  href = "#",
  isExternal = false,
  label =""
} = Astro.props
---

<a
  href={href}
  class={`text-white mt-5 text-xs md:text-sm font-normal border-2 border-[#14427c] rounded-full py-2 px-6 transition-all duration-300 ease-out transform hover:scale-110 hover:shadow-lg ${className}`}
  target={isExternal ? "_blank" : "_self"}
  aria-label= {label}
  rel={isExternal ? "noopener noreferrer" : undefined}
>
  {text}
</a>
